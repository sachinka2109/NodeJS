// we write the function without giving it a name

(a,b) => {
    return a+b;
}

/* We cannot call this function without assigning a variable.
    we are going to assign it a variable and then call the function
*/

let addNumbers = (a,b) => {
    return a+b;
}

console.log(addNumbers(5,6)); 

// Now, As you can see the noname function became a arrow-function.

// Example 2:

// Example 3:

// Example 4:

// Example 5:

// Example 6: