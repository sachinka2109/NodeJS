
// Writing a Normal Function to be executed when called

function addNumbers (a,b) { // we define normal functions with function keyword and give a name.In brackets we are setting 2 parameters
    return a+b; // we return a function
}

console.log(addNumbers(5,6)) // logging the answer by calling the function.